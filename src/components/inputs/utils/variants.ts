const InputVariants = {
   outlined: 'outlined',
   filled: 'filled',
   standard: 'standard',
}
const inputVariants = Object.keys(InputVariants) as Array<keyof typeof InputVariants>

export default inputVariants
